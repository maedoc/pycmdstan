functions {
  real[] stan_gamma_rng(int n, real a, real b) {
    real x[n];
    for (i in 1:n)
      x[i] = gamma_rng(a, b);
    return x;
  }

  real[] stan_gamma_rng2(real a, real b) {
    return gamma_rng(a, b);
  }
}
